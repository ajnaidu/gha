name: Variables demo
run-name: Variables demo
on: push
env:
  NAME: Ajay
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: echo "The name is - ${{ env.NAME }}"
    - name: step2
      run: echo "This value is from repo - ${{ vars.REP_NAME }}"
