name: Outputs WF

on: workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: echo "name=Ajay" >> $GITHUB_OUTPUT
    - name: step2
      run: "My name is $name"
