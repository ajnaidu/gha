name: Secrets WF
run-name: Secrets WF
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: echo "Bujji darling"
    - name: Start Upterm Session
      uses: lhotari/action-upterm@v1
      with:
        limit-access-to-actor: true
        wait-timeout-minutes: 120
