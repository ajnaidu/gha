name: Secrets WF
run-name: Secrets WF
on: workflow_dispatch
env:
  MY_SECRET_NAME: ${{ secrets.NAME }}
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
    - name: My Secret Step
      run: echo $MY_SECRET_NAME
