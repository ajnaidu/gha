name: sample workflow

on: workflow_dispatch

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
    - name: step1
      run: git clone https://github.com/ajnaidu/gha.git
    - name: step2
      run: mvn -v
